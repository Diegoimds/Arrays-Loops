const nomesDeEstudantes = ['jose', 'joao', 'maria', 'joana', 'andre', 'alice'];

//descobrir o tamanho de uma array - utilize o ".length" no final da array
console.log(nomesDeEstudantes.length);

//adicionar mais um index na útima posição da array
nomesDeEstudantes[nomesDeEstudantes.length] = "pedro"

console.log(nomesDeEstudantes);